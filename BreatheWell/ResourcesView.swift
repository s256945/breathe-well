import SwiftUI

struct ResourcesView: View {
    var body: some View {
        Text("Resources").padding()
    }
}

struct ProfileView: View {
    var body: some View {
        Text("Profile").padding()
    }
}
